server:
  port: 8888

spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/mahmoudelraey/spring-microservices-and-cloud/tree/master/common-config

eureka:
  client:
    register-with-eureka: true
    fetch-registry: true
