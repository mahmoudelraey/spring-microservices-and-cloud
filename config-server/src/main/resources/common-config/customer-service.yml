spring:
  datasource:
    url: jdbc:mysql://localhost:3307/customer_upgrade?serverTimezone=UTC
    username: root
    password: my-secret-pw
  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL55Dialect
    hibernate:
      ddl-auto: update
